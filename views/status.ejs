<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <title><%= message %></title>
    <style>
        body {
            background: linear-gradient(to right, #f8d8c0, #fffcee);
            height: 100vh; /* Adjust the height as needed */
            margin: 0;
            text-align: center;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 10vw;
        }

        div {
            border-radius: 15px;
            box-shadow: 0px 0px 30px 0px #ea995a;
        }

        i {
            animation: vibrate 0.2s 3;
        }

        @keyframes vibrate {
            0%, 100% {
                transform: translate(0);
            }
            20% {
                transform: translate(-5px, 0);
            }
            40% {
                transform: translate(5px, 0);
            }
            60% {
                transform: translate(-5px, 0);
            }
            80% {
                transform: translate(5px, 0);
            }
        }
    </style>
</head>
<body>
    <div class="bg-light p-5 mx-5">
        <% if (message === "Email Already Validated") { %>
            <i class="fa fa-envelope"></i>
        <% } else if (message === "Email Validated Successfully") { %>
            <i class="text-success fa fa-check"></i>
        <% } else {%> <i class="text-danger fa fa-times"></i> <%}
        %>
        <h1><%= message %></h1>
    </div>
</body>
</html>
